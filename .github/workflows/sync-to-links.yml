name: Sync to starrydata.github.io/links/ and /hp/

# starrydata.nims.go.jp/ (NIMS プロキシ) が指している
# starrydata.github.io/links/ と starrydata.github.io/hp/ を同時に同期する。
#
# /links/ … 旧URL (starrydata.nims.go.jp/links/research/ 等) を維持
# /hp/    … 新URL (starrydata.nims.go.jp/hp/research/ 等) を並走させる
#
# 必要な設定:
#   - Secrets → Actions → PAGES_SYNC_TOKEN
#     (fine-grained PAT: starrydata/starrydata.github.io の Contents Read/Write のみ)

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: sync-links
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source (starrydata_HP_nims)
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build with /links/ prefix (旧URL維持)
        env:
          PATHPREFIX: "/links/"
        run: npx eleventy --output=_site_links

      - name: Build with /hp/ prefix (新URL)
        env:
          PATHPREFIX: "/hp/"
        run: npx eleventy --output=_site_hp

      - name: Checkout target (starrydata.github.io)
        uses: actions/checkout@v4
        with:
          repository: starrydata/starrydata.github.io
          token: ${{ secrets.PAGES_SYNC_TOKEN }}
          path: target

      - name: Replace links/ and hp/ contents
        run: |
          rm -rf target/links
          mkdir -p target/links
          cp -R _site_links/. target/links/
          rm -rf target/hp
          mkdir -p target/hp
          cp -R _site_hp/. target/hp/

      - name: Commit & push if changed
        run: |
          cd target
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add links hp
          if git diff --cached --quiet; then
            echo "No changes to sync."
            exit 0
          fi
          SRC_SHA="${GITHUB_SHA::7}"
          git commit -m "Sync links/ and hp/ from starrydata_HP_nims @ ${SRC_SHA}"
          git push origin main
