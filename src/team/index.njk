---
layout: modern.njk
permalink: /team/index.html
title: メンバー
lang: ja
---

{%- set L = "ja" -%}

<!-- =================== PAGE HEAD =================== -->
<section class="page-head">
  <div class="container">
    <span class="eyebrow">Team</span>
    <h1>Members</h1>
    <p class="lead">{{ i18n.team.lead[L] }}</p>
  </div>
</section>

<!-- =================== MEMBERS =================== -->
<section class="section section-top-tight">
  <div class="container">
    {%- for group in members.groups %}
      {%- if group.members | length > 0 %}
      <div class="team-group" data-group="{{ group.key }}">
        {% if group.label %}
        <h2 class="team-group-title">
          {{ group.label }}
          {% if group.label_en and group.label_en != group.label %}<span class="en">{{ group.label_en }}</span>{% endif %}
          <span class="count">{{ group.members | length }}</span>
        </h2>
        {% endif %}

        {% if group.key == "alumni" %}
        <!-- Alumni はテキストリスト -->
        <ul class="alumni-list">
          {%- for m in group.members %}
          <li>
            <span class="name-ja">{{ m.name_ja }}</span>
            <span class="name-en">{{ m.name_en }}</span>
          </li>
          {%- endfor %}
        </ul>
        {% else %}
        <div class="team-page-grid">
          {%- for m in group.members %}
          <article class="team-page-card">
            <div class="photo">
              {% if m.photo %}
                <img src="{{ m.photo }}" alt="{{ m.name_ja }}" loading="lazy">
              {% else %}
                <div class="photo-placeholder">{{ m.name_ja | first }}</div>
              {% endif %}
            </div>
            <div class="info">
              <h3 class="name">{{ m.name_ja }}<span class="en">{{ m.name_en }}</span></h3>
              {% if m.role %}
              <p class="role">{{ m.role | replace("\n", "<br>") | safe }}</p>
              {% endif %}
              {% if m.affiliation %}
              <p class="aff">{{ m.affiliation | replace("\n", "<br>") | safe }}</p>
              {% endif %}
              {% if m.links | length > 0 %}
              <ul class="links">
                {%- for l in m.links %}
                <li><a href="{{ l.href }}" target="_blank" rel="noopener">{{ l.label }}</a></li>
                {%- endfor %}
              </ul>
              {% endif %}
            </div>
          </article>
          {%- endfor %}
        </div>
        {% endif %}
      </div>
      {%- endif %}
    {%- endfor %}
  </div>
</section>
